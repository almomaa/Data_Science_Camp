function d = hammingDistance(seq1, seq2)
%HAMMINGDISTANCE Count positions that differ between two equal-length sequences.
%   d = hammingDistance(seq1, seq2)
%   This teaching version ignores positions marked N in either sequence.

seq1 = upper(char(seq1));
seq2 = upper(char(seq2));


end
